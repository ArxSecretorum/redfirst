# Abandoned copy of the pipeline. Nothing imports this module any more, but the
# name is still written here — which used to be enough to call Ghost alive.
class Ghost:
    pass


def build():
    return Ghost()
