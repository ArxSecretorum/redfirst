# напр ~ код ~ подстрока ~ команда ~ происхождение
red ~ 0 ~ .exe\" -l ~ @sh sh "$INSTALL" . >/dev/null 2>&1; cat .claude/settings.json ~ 2026-08-17: на POSIX-эмуляции поверх Windows интерпретатор в хуке обязан запускаться как логин-оболочка, иначе он не найдёт даже mktemp. Стоит 427 мс против 189 мс, платится один раз за сессию
