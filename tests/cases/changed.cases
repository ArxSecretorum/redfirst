# changed — с чего начать проверку, когда имени вам никто не назвал.
# id ~ напр ~ код ~ подстрока ~ стенд ~ команда ~ происхождение

changed-not-a-repo   ~ red ~ 2 ~ not a git repository ~ - ~ changed ~ 2026-08-17: без истории сравнивать не с чем, и это отказ, а не пустой ответ
changed-unknown-ref  ~ red ~ 2 ~ comparison point not found ~ - ~ @sh git init -q . >/dev/null 2>&1; sh "$RF" changed НетТакойВетки ~ 2026-08-17: опечатка в имени ветки не должна выглядеть как «изменений нет»
