# напр ~ код ~ подстрока ~ команда ~ происхождение
red ~ * ~ left=0 ~ @sh export TMPDIR="$PWD/tmpd"; mkdir -p "$TMPDIR"; sh "$RF" counter Live 2>/dev/null | head -n 1 >/dev/null; sleep 1; printf 'left=%s\n' "$(ls "$TMPDIR" | wc -l | tr -d ' ')" ~ 2026-08-17: оборванный вывод убивает инструмент сигналом PIPE, ловушка его не ловит, и на целевой машине остаётся пустой каталог. Инструмент, который сорит в /tmp чужой машины, к доверию не располагает
