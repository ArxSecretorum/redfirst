# Золотой корпус — настоящий код, вердикт проверен глазами.
# id ~ напр ~ код ~ подстрока ~ стенд ~ команда ~ происхождение
#
# Синтетические стенды не поймали ни одного из двух ложных вердиктов ниже.
# Поймал живой проект — поэтому здесь форма реального кода, а не выдуманная.
# Пометка `?` означает: направление верное, инструмент его сегодня не выполняет.

# ── два ложных вердикта, в обе стороны сразу ────────────────────────────────

golden-extension-dead   ~ red?   ~ 1 ~ Ноль вызовов && n:decl=1 && n:call=0 ~ @golden ~ wired selectionTier ~ 2026-08-17: extension-функция. `fun MediaController.selectionTier(` — между `fun` и именем стоит получатель, объявление не опознаётся и идёт в вызовы. Правда: мёртв, единственное вхождение — само объявление
golden-template-live    ~ green? ~ 0 ~ ОСТАЁТСЯ ВЫЗОВОВ && n:call=2 ~ @golden ~ wired identityFingerprintSha256 ~ 2026-08-17: оба вызова стоят внутри шаблонов строк Kotlin "${store.identityFingerprintSha256()}", strip_strings их вычищает. Правда: жив, два вызова

# ── контроль: эти два инструмент определяет верно и обязан определять дальше ─

golden-ringlog-dead     ~ red   ~ 1 ~ Ноль вызовов && n:total=1 && n:decl=1 && n:call=0 && sum ~ @golden ~ wired DiagnosticRingLog ~ 2026-08-17: мёртвый класс, 287 строк в оригинале. Правда: мёртв
golden-playback-live    ~ green ~ 0 ~ ОСТАЁТСЯ ВЫЗОВОВ && n:total=7 && n:decl=1 && n:text=3 && n:call=3 && sum ~ @golden ~ wired PlaybackService ~ 2026-08-17: три вызова в двух файлах плюс три упоминания в комментариях. Правда: жив
